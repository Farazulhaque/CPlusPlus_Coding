#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int x, y, z, a, b, d, g, y;

    g = (x + 2) / pow(z, 2);

    y = pow(x, 2) / pow(z, 2);

    a = (pow(x, 2) + pow(y, 3) + pow(z, 4)) / (pow(a + b, 2) - d);

    return 0;
}